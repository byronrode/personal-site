{{#get "posts" limit="all" filter="tags:[{{tags}}]+id:-{{id}}" as |related|}}
  {{log related}}
    {{#if related}}
        <section class="related-posts">
            <div class="container">
                <h3 class="related-title">You Might Also Like...</h3>
                <div class="post-feed">
                    {{#foreach posts}}
                        <div class="grid-item grid-sizer"></div>
                        {{> "loop"}}
                    {{/foreach}}
                </div>
            </div>
        </section>
    {{/if}}
{{/get}}